let person = {
    name: 'Zee',
    age: 25
};
// Dot Notation
person.name = 'John'; // For this we can use console.log(person.name);
// For running the complete object just use console.log(person);
// Bracket Notation
person['name'] = 'Mary';
console.log(person['name']);